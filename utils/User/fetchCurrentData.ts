// import { doc, getDoc } from "firebase/firestore"
// import { db } from "../../firebaseConfig"

// interface IProps {
//     id: string | undefined
// }

// export const fetchCurrentData = async ( {id}:IProps ) => {
//     const userRef = doc(db, "users", id as string)
//     const data = await getDoc(userRef)
//     return data.data()
// }

export {}